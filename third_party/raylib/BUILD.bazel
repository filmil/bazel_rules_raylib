
genrule(
    name = "glxinfo",
    outs = [ "glxinfo.txt"],
    cmd = "ls /tmp/.X11-unix && env DISPLAY=:0 strace glxinfo > $@",
)

genrule(
    name = "xeyes",
    outs = [ "xeyes.txt"],
    cmd = "ls /tmp/.X11-unix && env DISPLAY=:0 strace xeyes > $@",
)
